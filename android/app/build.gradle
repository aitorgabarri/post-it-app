apply plugin: 'com.android.application'

android {
    namespace "com.tuapp.posit"
    compileSdk 36

    defaultConfig {
        applicationId "com.tuapp.posit"
        minSdk 24
        targetSdk 36
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    // Esto corrige el error de "Java version 8 is obsolete"
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])

    // Estas son las piezas que le faltaban a tu MainActivity.java
    implementation 'androidx.appcompat:appcompat:1.7.0'
    implementation 'com.google.android.material:material:1.12.0'

    implementation project(':capacitor-android')
    implementation project(':capacitor-cordova-android-plugins')
}

// Seguimos ignorando este archivo para evitar errores
// apply from: 'capacitor.build.gradle'